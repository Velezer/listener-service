# Listener Service

## Running tests

Use unit tests for fast feedback during development:

```bash
cd android
./gradlew test
```

Run instrumentation tests when you need Android runtime/device integration coverage:

```bash
cd android
./gradlew connectedAndroidTest
```

